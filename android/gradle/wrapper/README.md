`gradle-wrapper.jar` bu repoda su anda yok.

Yerelde olusturmak icin:

1. `cd android`
2. `gradle wrapper`

Bu komut `gradle/wrapper/gradle-wrapper.jar` dosyasini olusturur ve `./gradlew` komutunu calisir hale getirir.
